name = "design-system"
compatibility_date = "2024-07-18"

# Static-assets Worker: serves the Storybook static build.
# Build command (set in Cloudflare dashboard): npm run build-storybook
# Deploy command (default): npx wrangler deploy
[assets]
directory = "./storybook-static"
not_found_handling = "404-page"

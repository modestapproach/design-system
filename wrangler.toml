name = "design-system"
compatibility_date = "2024-07-18"

# This project deploys the Storybook static build to Cloudflare Pages.
# Build command:   npm run build-storybook
# Output directory: storybook-static
pages_build_output_dir = "storybook-static"
